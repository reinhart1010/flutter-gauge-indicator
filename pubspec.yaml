name: gauge_indicator
description: An animated, highly customizable, opensource, Flutter gauge widgets.
version: 0.2.1
homepage: https://github.com/HTD-Health/gauge_indicator

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  collection: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
